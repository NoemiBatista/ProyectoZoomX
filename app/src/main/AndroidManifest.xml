<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectozoomx">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".BuscarActivity"></activity>
        <activity android:name=".EliminarActivity" />
        <activity android:name=".ModificarActivity" />
        <activity android:name=".ConfigActivity" />
        <activity android:name=".IngresoSalaActivity" />
        <activity android:name=".ZoomMenuPrincipalActivity" />
        <activity android:name=".ZoomLoginActivity" />
    </application>

</manifest>